version: 2

sources:
  - name: bronze
    database: tennis_dev
    schema: bronze
    description: "Raw ingested data — Bronze layer"
    tables:
      - name: raw_students
        description: "Student profiles ingested from CSV"
      - name: raw_match_extractions
        description: "Structured match stats extracted by Claude API"
      - name: raw_coaching_notes
        description: "Raw markdown coaching notes"
      - name: raw_intake_events
        description: "Student intake form submissions from Typeform via Kafka"